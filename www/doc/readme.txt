解决自治体行为冲突的关键点：
1、碰撞避免，从一开始设定target开始，避免设定和其他unit相冲突的target。
2、碰撞避免，设定unit群体范围(neighbor_radius)，观察周围的邻居。
3、计算unit触角(自己在未来几部内会到达的point)，然后看看是否和周围的邻居相交，如果相交，采取避免策略。
4、避免策略：不动，左绕，右绕，后退，等等。
5、避免进入碰撞僵持，在一定程度上允许unit重叠。
6、采取被动寻路方式，快要碰到了才开始寻路。

一定要用鼠标/触控操作。
